1. Create python venv

`# python -m venv venv`

2. Activate venv

`# source venv/bin/activate`

3. Install dependencies

`# pip install -r requirements.txt`

4. Run `make`

`# make`

5. Open `dump.gtkw` file with `gtkwave`
